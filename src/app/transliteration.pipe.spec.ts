import { TransliterationPipe } from './transliteration.pipe';

describe('TransliterationPipe', () => {
  it('create an instance', () => {
    const pipe = new TransliterationPipe();
    expect(pipe).toBeTruthy();
  });
});
